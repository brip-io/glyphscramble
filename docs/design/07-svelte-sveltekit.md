# 07 · Svelte and SvelteKit

Status: beta API implemented. The Svelte 5 action/component consumes encoded payloads; the SvelteKit handle creates locals, serves fonts, and applies no-store headers. Remaining release gate: package-consumer compilation and streaming navigation fixtures.
