# 05 · React and Next

Status: beta API implemented. The client component accepts only `GlyphPayload`; a server-only helper owns plaintext. Initializer output includes the engine singleton, App Router font route, and Next 16 `proxy.ts`. Remaining release gate: a deployable example covering RSC streaming and client navigation.
