---
"@brip/glyphscramble": minor
---

Generate beta, exact-version, and stable package commands from one public
package-surface contract, and make `init` pin every missing GlyphScramble
package to the running CLI version so prerelease adapters cannot drift to
`latest` or another beta.
